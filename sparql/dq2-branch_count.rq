# DQ2. Count of diseases in each branch (diseases may be in multiple branches)
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX obo: <http://purl.obolibrary.org/obo/>

SELECT ?branch (COUNT(?class) AS ?count)
WHERE {
    ?class rdfs:subClassOf* ?branch_class .
    ?branch_class rdfs:subClassOf obo:DOID_4 ;
        rdfs:label ?branch .
}
GROUP BY ?branch
ORDER BY ?branch